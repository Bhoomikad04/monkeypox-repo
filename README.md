# Monkeypox Streamlit App — Containerized

## Prepare (one-time)
From the directory where you put the repo files:

```bash
# ensure models are present
ls -la models/
# check app runs locally with your current Python env (optional)
pip install -r requirements.txt
streamlit run app.py
